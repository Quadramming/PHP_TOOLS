<?php
	//================================================================
	// Settings
	//================================================================

	$input  = 'QQ_output.txt';

	//================================================================
	// Script
	//================================================================
	
	$inputFile = @file($input);
	
	if ( $inputFile == FALSE ) {
		die("There sould be $input file.");
	}

	foreach ( $inputFile as $line ) {
		if ( $line[0] != '=' ) {
			if ( $line[0] == 'D' ) {
				unlink( substr(trim($line), 2) );
			}
		}
	}
	
	//================================================================
	// Common functions
	//================================================================	
	
?>
